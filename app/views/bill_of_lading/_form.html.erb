<%= form_for(@bill_of_lading) do |f| %>
  <% if @bill_of_lading.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bill_of_lading.errors.count, "error") %> prohibited this bill_of_lading from being saved:</h2>

      <ul>
      <% @bill_of_lading.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <label for="exampleInputEmail1">Bl Number</label>
    <%= f.text_field :bl_number, :step => 'any', class: 'form-control' %>
  </div>
  <div class="form-group">
    <label for="exampleInputEmail1">Bl Date</label>
    <%= f.date_select :bl_date, :step => 'any', class: 'form-control' %>
  </div>

  <div class="box-footer">
    <%= f.submit "Submit", class: "btn btn-success btn-sm" %>
      <!--<button type="submit" class="btn btn-primary">Submit</button>-->
  </div> 
  <% end %>
