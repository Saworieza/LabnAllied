<%= form_for(@insurance) do |f| %>
  <% if @insurance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@insurance.errors.count, "error") %> prohibited this insurance from being saved:</h2>

      <ul>
      <% @insurance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <label for="exampleInputFile">Insurance Name</label>
    <%= f.text_field :insurance_name, :step => 'any', class: 'form-control' %>
  </div>
  <div class="form-group">
    <label for="exampleInputFile">Total Insure Charges</label>
    <%= f.text_field :total_insurance_charges, :step => 'any', class: 'form-control' %>
  </div>
  <div class="form-group">
    <label for="exampleInputFile">Attachments</label>
    <%#= f.file_field :avatar %>
    <%= f.file_field :avatar, :step => 'any' %>
  </div>

  <!--<div class="form-group">
    <label for="exampleInputFile">Attachments</label>
    <%= f.file_field :avatar, :step => 'any', class: 'form-control' %>
  </div>-->

  <div class="box-footer">
    <%= f.submit "Submit", class: "btn btn-success btn-sm" %>
      <!--<button type="submit" class="btn btn-primary">Submit</button>-->
  </div>
    <%#= link_to 'Next Form', shipments_path %>
<% end %>
