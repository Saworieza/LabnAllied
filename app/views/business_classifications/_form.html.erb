<%= form_for(@business_classification) do |f| %>
  <% if @business_classification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@business_classification.errors.count, "error") %> prohibited this business_classification from being saved:</h2>

      <ul>
      <% @business_classification.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <label for="exampleInputPassword1">Business Classification</label>
    <%= f.text_field :business_classification, :step => 'any', class: 'form-control'%>
  </div>
  <div class="box-footer">
    <%= f.submit "Submit", class: "btn btn-success btn-sm" %>
      <!--<button type="submit" class="btn btn-primary">Submit</button>-->
  </div>  
<% end %>
